---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
---

<BaseLayout
  title="Oplossingen voor restaurants — Tastory"
  description="Ontdek hoe Tastory restaurants helpt met zichtbaarheid, imago en terugkerende gasten."
>
  <PageHero
    tag="Restaurants"
    title="Ontdek Tastory, de app die met een representatief restaurantprofiel terugkerende gasten <em>realiseert</em>"
    description="Tastory helpt restaurants gasten aan te trekken die oprecht jouw concept waarderen, door middel van authentieke en representatieve restaurantprofielen."
  />

  <section>
    <div class="reveal">
      <div class="section-tag">Wat je bereikt</div>
      <h2 class="section-title">Vier doelen, &eacute;&eacute;n <em>app</em></h2>
    </div>
    <div class="goals-grid">
      <div class="goal-card reveal">
        <div class="goal-number">01</div>
        <h3>Meer zichtbaarheid</h3>
        <p>
          Word beter zichtbaar door actuele en professionele content op je restaurantprofiel.
        </p>
      </div>
      <div class="goal-card reveal">
        <div class="goal-number">02</div>
        <h3>Representatief imago</h3>
        <p>
          Vertel het verhaal van jouw restaurant via short-form content en foto's. Gebruik je missie
          en visie om nieuwe gasten aan te trekken.
        </p>
      </div>
      <div class="goal-card reveal">
        <div class="goal-number">03</div>
        <h3>Terugkerende gasten</h3>
        <p>
          Gebruik inzichten en gastervaringen om je profiel representatiever te maken, zodat
          verwachtingen overeenkomen met de werkelijkheid.
        </p>
      </div>
      <div class="goal-card reveal">
        <div class="goal-number">04</div>
        <h3>Betere reviews</h3>
        <p>
          Gebruik het Tastory-dashboard om verbeteringen door te voeren en ervaringen te verbeteren.
        </p>
      </div>
    </div>
  </section>

  <section class="pricing-section">
    <div class="reveal">
      <div class="section-tag">Pakketten</div>
      <h2 class="section-title">Kies het pakket dat bij jou <em>past</em></h2>
    </div>
    <div class="pricing-grid">
      <div class="pricing-card reveal">
        <h3>Chefs Keuze</h3>
        <ul>
          <li>Kwalitatieve foto's en video's*</li>
          <li>Marketingplan</li>
          <li>Extra zichtbaarheid in de app</li>
          <li>Alles uit 3-gangen</li>
        </ul>
        <p class="footnote">* Kwalitatieve foto's en video worden geschoten door partner van Tastory.</p>
      </div>
      <div class="pricing-card pricing-card--featured reveal">
        <div class="pricing-badge">Populair</div>
        <h3>3-Gangen Menu</h3>
        <ul>
          <li>Kwalitatieve foto's*</li>
          <li>Reservering klik-inzichten</li>
          <li>Alles uit Proeverij</li>
        </ul>
        <p class="footnote">* Kwalitatieve foto's worden geschoten door partner van Tastory.</p>
      </div>
      <div class="pricing-card reveal">
        <h3>Proeverij</h3>
        <ul>
          <li>App-plaatsing</li>
          <li>Coming-soon portaal toegang</li>
          <li>Volledig profielbeheer in huisstijl</li>
          <li>Zelf content aanleveren*</li>
        </ul>
        <p class="footnote">* Jij levert jouw unieke foto's en video's aan volgens de richtlijnen en voorwaarden van Tastory. Zo houden wij de app objectief en creëren wij een uniforme standaard.</p>
      </div>
    </div>
  </section>

  <section>
    <div class="reveal">
      <div class="section-tag">Professionele content</div>
      <h2 class="section-title">
        Short-form video's die jouw restaurant <em>tot leven brengen</em>
      </h2>
    </div>
    <div class="content-info reveal">
      <p>
        Tastory werkt samen met gespecialiseerde contentmakers om de identiteit van jouw restaurant
        vast te leggen in professionele short-form content video's van 30-60 seconden. Deze
        video's presenteren jouw restaurant of specifieke marketingelementen op een authentieke en
        aantrekkelijke manier.
      </p>
    </div>
  </section>

  <section class="pillars-section">
    <div class="reveal">
      <div class="section-tag">Fundament</div>
      <h2 class="section-title">Vijf-pijler <em>marketingplan</em></h2>
    </div>
    <div class="pillars-grid">
      <div class="pillar reveal"><span class="pillar-num">1</span><span>De 'why'</span></div>
      <div class="pillar reveal"><span class="pillar-num">2</span><span>Gewenst imago</span></div>
      <div class="pillar reveal">
        <span class="pillar-num">3</span><span>Publieke perceptie</span>
      </div>
      <div class="pillar reveal"><span class="pillar-num">4</span><span>Doelgroep</span></div>
      <div class="pillar reveal"><span class="pillar-num">5</span><span>KPI's</span></div>
    </div>
  </section>
</BaseLayout>

<style>
  .goals-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .goal-card {
    background: var(--white);
    border: 1px solid rgba(136, 101, 69, 0.06);
    border-radius: var(--radius-md);
    padding: 2.5rem;
  }

  .goal-number {
    font-family: var(--serif);
    font-size: var(--text-3xl);
    color: var(--orange);
    margin-bottom: 1rem;
    opacity: 0.5;
  }

  .goal-card h3 {
    font-family: var(--serif);
    font-size: 1.3rem;
    color: var(--text);
    margin-bottom: 0.8rem;
  }

  .goal-card p {
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--text-muted);
  }

  /* Pricing */
  .pricing-section {
    background: var(--white);
    padding: 7rem 3rem;
    max-width: 100%;
  }

  .pricing-section > .reveal {
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 3rem;
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .pricing-card {
    background: var(--beige);
    border: 1px solid rgba(136, 101, 69, 0.08);
    border-radius: 20px;
    padding: 2.5rem;
    position: relative;
  }

  .pricing-card--featured {
    border-color: var(--orange);
    box-shadow: 0 10px 40px rgba(255, 142, 62, 0.12);
  }

  .pricing-badge {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--orange);
    color: var(--white);
    padding: 0.3rem 1.2rem;
    border-radius: 100px;
    font-size: var(--text-base);
    font-weight: 600;
  }

  .pricing-card h3 {
    font-family: var(--serif);
    font-size: 1.4rem;
    color: var(--text);
    margin-bottom: 1rem;
  }

  .pricing-card ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .pricing-card li {
    font-size: var(--text-base);
    color: var(--text);
    padding-left: 1.5rem;
    position: relative;
  }

  .pricing-card li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--orange);
    font-weight: 600;
  }

  .footnote {
    font-size: 0.8rem;
    color: var(--text-muted);
    margin-top: 1rem;
    line-height: 1.4;
  }

  .content-info {
    max-width: 700px;
    margin-top: 2rem;
  }

  .content-info p {
    font-size: 1.05rem;
    line-height: 1.8;
    color: var(--text-muted);
  }

  /* Pillars */
  .pillars-section {
    background: var(--white);
    padding: 7rem 3rem;
    max-width: 100%;
  }

  .pillars-section > .reveal {
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .pillars-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
    margin-top: 3rem;
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .pillar {
    background: var(--beige);
    border: 1px solid rgba(136, 101, 69, 0.08);
    border-radius: var(--radius-md);
    padding: 2rem 1.5rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.8rem;
    font-size: var(--text-base);
    color: var(--text);
    font-weight: 500;
  }

  .pillar-num {
    font-family: var(--serif);
    font-size: 1.8rem;
    color: var(--orange);
  }

  @media (max-width: 1024px) {
    .goals-grid {
      grid-template-columns: 1fr;
    }
    .pricing-grid {
      grid-template-columns: 1fr;
    }
    .pillars-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 640px) {
    .pillars-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
