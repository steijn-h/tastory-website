---
const base = import.meta.env.BASE_URL;
---

<section class="hero" id="hero">
  <div class="hero-sticky">
    <div class="hero-bg">
      <div class="hero-orb hero-orb-1"></div>
      <div class="hero-orb hero-orb-2"></div>
      <div class="hero-orb hero-orb-3"></div>
    </div>

    <div class="hero-content">
    <div class="hero-text">
      <div class="hero-badge">
        <span class="hero-badge-dot"></span>
        In 2026 live
      </div>
      <h1>Find it. Taste it. <em>Share it.</em></h1>
      <p class="hero-desc">
        De ultieme smaakvolle verbinding tussen gast en restaurant. Via Tastory kunnen restaurants
        zich beter en eerlijker presenteren, terwijl gebruikers snel en eenvoudig restaurants
        ontdekken die écht bij hen passen.
      </p>
      <div class="hero-actions">
        <a href="#download" class="btn-primary">Ontdek de mogelijkheden</a>
        <a href="#oplossingen" class="btn-secondary">Meer informatie</a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="phone-glow"></div>
      <div class="phone-wrapper">
        <div class="phone-mockup">
          <div class="phone-notch"></div>
          <div class="phone-screen" id="phone-screen">
            <img
              src={`${base}/homepage-long-screenshot.png`}
              alt="Tastory app screenshot"
              class="phone-screenshot"
              id="phone-screenshot"
              width="1170"
              height="4944"
              loading="eager"
            />
          </div>
        </div>
        <div class="float-card float-card-1">
          <div class="fc-label">Betrouwbare reviews</div>
        </div>
        <div class="float-card float-card-2">
          <div class="fc-label">Jouw verhaal</div>
        </div>
        <div class="float-card float-card-3">
          <div class="fc-label">Actuele content</div>
        </div>
      </div>
    </div>
  </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    height: auto;
    max-width: 100%;
    padding: 0;
    margin: 0;
    background: linear-gradient(
      160deg,
      #2e2318 0%,
      #3d2f1f 25%,
      #4a3828 50%,
      #3a2e22 75%,
      #2e2318 100%
    );
  }

  .hero-sticky {
    position: relative;
    top: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding: 9rem 3rem 5rem;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    background: linear-gradient(
      160deg,
      #2e2318 0%,
      #3d2f1f 25%,
      #4a3828 50%,
      #3a2e22 75%,
      #2e2318 100%
    );
  }

  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
  }

  .hero-orb-1 {
    width: 600px;
    height: 600px;
    background: rgba(255, 142, 62, 0.07);
    top: -10%;
    right: 10%;
  }
  .hero-orb-2 {
    width: 400px;
    height: 400px;
    background: rgba(136, 101, 69, 0.08);
    bottom: -15%;
    left: 5%;
  }
  .hero-orb-3 {
    width: 300px;
    height: 300px;
    background: rgba(255, 142, 62, 0.04);
    top: 50%;
    left: 40%;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
    width: 100%;
    display: grid;
    grid-template-columns: 1.15fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-text {
    animation: fadeUp 1s 0.2s ease both;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: rgba(255, 142, 62, 0.12);
    backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 142, 62, 0.25);
    border-radius: 100px;
    padding: 0.5rem 1.4rem 0.5rem 0.8rem;
    margin-bottom: 2rem;
    font-size: 1rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--orange-light);
    font-weight: 500;
  }

  .hero-badge-dot {
    width: 8px;
    height: 8px;
    background: var(--orange);
    border-radius: 50%;
    animation: blink 2s ease-in-out infinite;
    box-shadow: 0 0 8px rgba(255, 142, 62, 0.6);
  }

  .hero h1 {
    font-family: var(--serif);
    font-size: clamp(3rem, 5vw, 5rem);
    line-height: 1.08;
    color: var(--white);
    font-weight: 400;
    margin-bottom: 1.8rem;
    text-shadow: 0 2px 30px rgba(58, 46, 34, 0.3);
  }

  .hero h1 em {
    color: var(--orange);
    font-style: italic;
  }

  .hero-desc {
    font-size: 1.15rem;
    line-height: 1.85;
    color: rgba(255, 252, 248, 0.78);
    max-width: 520px;
    margin-bottom: 2.8rem;
  }

  .hero-actions {
    display: flex;
    gap: 1.2rem;
    align-items: center;
    flex-wrap: wrap;
  }

  /* Phone */
  .hero-visual {
    display: flex;
    justify-content: center;
    animation: fadeUp 1s 0.5s ease both;
    position: relative;
  }

  .phone-glow {
    position: absolute;
    width: 350px;
    height: 350px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, rgba(255, 142, 62, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(40px);
    pointer-events: none;
    z-index: 0;
  }

  .phone-wrapper {
    position: relative;
    z-index: 1;
  }

  .phone-mockup {
    width: 300px;
    height: clamp(580px, calc(100vh - 14rem), 720px);
    background: #1a1714;
    border-radius: 44px;
    position: relative;
    overflow: hidden;
    box-shadow:
      0 60px 120px rgba(0, 0, 0, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.04),
      0 0 80px rgba(255, 142, 62, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.06);
  }

  .phone-notch {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 26px;
    background: #0e0c0a;
    border-radius: 0 0 18px 18px;
    z-index: 5;
  }

  .phone-screen {
    position: absolute;
    inset: 5px;
    border-radius: 40px;
    overflow: hidden;
    background: #161310;
  }

  .phone-screenshot {
    width: 100%;
    height: auto;
    display: block;
    object-position: top;
    vertical-align: top;
  }

  /* Float cards */
  .float-card {
    position: absolute;
    background: rgba(255, 252, 248, 0.95);
    backdrop-filter: blur(24px);
    border: 1.5px solid rgba(255, 142, 62, 0.35);
    border-radius: var(--radius-sm);
    padding: 0.4rem 0.75rem;
    animation: float 6s ease-in-out infinite;
    white-space: nowrap;
    box-shadow:
      0 4px 12px rgba(58, 46, 34, 0.08),
      0 12px 40px rgba(58, 46, 34, 0.1),
      0 0 0 1px rgba(255, 142, 62, 0.08);
  }

  .float-card-1 {
    top: 22%;
    right: -50px;
    animation-delay: 0s;
  }
  .float-card-2 {
    bottom: 16%;
    left: -45px;
    animation-delay: 2s;
  }
  .float-card-3 {
    top: 52%;
    right: -60px;
    animation-delay: 4s;
  }

  .fc-label {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text);
    font-weight: 500;
  }

  @media (max-width: 1024px) {
    .hero {
      height: auto;
    }

    .hero-sticky {
      position: relative;
      height: auto;
      overflow: visible;
      padding: 7rem 2rem 4rem;
    }

    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2.5rem;
    }

    .hero-desc {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-badge {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-visual {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .phone-mockup {
      width: 240px;
      height: 500px;
      border-radius: 38px;
    }

    .phone-glow {
      width: 280px;
      height: 280px;
    }

    .float-card {
      padding: 0.3rem 0.6rem;
    }

    .fc-label {
      font-size: 0.65rem;
    }
  }

  @media (max-width: 640px) {
    .hero-sticky {
      padding: 6rem 1.5rem 3rem;
    }

    .phone-mockup {
      width: 220px;
      height: 460px;
      border-radius: 36px;
    }

    .phone-notch {
      width: 80px;
      height: 22px;
      border-radius: 0 0 14px 14px;
    }

    .phone-screen {
      border-radius: 32px;
    }

    .float-card {
      padding: 0.25rem 0.5rem;
    }

    .float-card-1 {
      right: -35px;
    }

    .float-card-2 {
      left: -32px;
    }

    .float-card-3 {
      right: -38px;
    }

    .fc-label {
      font-size: 0.6rem;
    }
  }
</style>

